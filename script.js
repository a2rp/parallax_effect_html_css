window.onload = () => {
    // window.onscroll = (event) => {
    //     let top = window.scrollY * 0.05;
    //     top = top < 1 ? 1 : window.scrollY;
    //     document.querySelector(".background-image").style.cssText = `
    //         transform: scale(${top});
    //     `;
    // };
};

